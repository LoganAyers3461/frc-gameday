"use client";
import { useEffect, useState } from "react";
import { compactMatchLabel } from "@/lib/gameday/matchUtils";
export default function TeamPill({ team, status, teamCount, nextMatch }) { const [,tick]=useState(0); useEffect(()=>{if(!nextMatch?.predicted_time)return; const id=window.setInterval(()=>tick(v=>v+1),1000); return()=>window.clearInterval(id)},[nextMatch?.predicted_time]); const r=status?.qual?.ranking?.record; const countdown=nextMatch?.predicted_time?Math.max(0,Math.round(nextMatch.predicted_time-Date.now()/1000)):null; return <div className="flex h-7 items-center gap-1.5 rounded-md border border-white/10 bg-neutral-950/90 px-2 shadow-lg backdrop-blur"><span className="font-mono text-[11px] font-bold">{String(team).replace(/^frc/i,"")}</span><span className="font-mono text-[9px] text-neutral-400">{r?`${r.wins??0}-${r.losses??0}-${r.ties??0}`:"—"}</span><span className="font-mono text-[9px] text-neutral-500">{status?.qual?.ranking?.rank?`#${status.qual.ranking.rank}/${teamCount||"?"}`:"—"}</span>{nextMatch&&<><span className="h-3 w-px bg-white/10"/><span className="font-mono text-[10px] font-bold">{compactMatchLabel(nextMatch)}</span>{countdown!=null&&<span className="font-mono text-[9px] text-neutral-400">{countdown<60?`${countdown}s`:`${Math.ceil(countdown/60)}m`}</span>}</>}</div>; }
